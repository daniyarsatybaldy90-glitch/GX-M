# GX-M Кодирование — Реализация

## 📋 Обзор

Реализована полная система автоматического генерирования кодов GX-моделей по формату:

```
GX-M[XXX]-[R]-[C]-[IP]-[UC]-[PRT]
```

Код обновляется **динамически** при заполнении формы конфигуратора.

---

## 🔧 Формат кодирования

| Компонент | Код | Описание | Значения |
|-----------|-----|---------|----------|
| **Серия** | `GX-M` | Идентификатор серии | - |
| **Ток** | `[XXX]` | Номинальный ток в амперах | 100, 250, 400, 630, 800, 1000, 1250, 1600 |
| **Расцепитель** | `[R]` | Тип расцепителя | `TM` (термомагнитный), `EM` (электронный) |
| **Корпус** | `[C]` | Материал и исполнение | `CS` (сталь), `NS` (нерж. сталь), `EX` (взрывозащита) |
| **IP** | `[IP]` | Степень защиты | `IP40`, `IP54`, `IP65`, `IP66` |
| **Управление** | `[UC]` | Тип управления | `ML` (местное), `RD` (дистанционное), `FM` (комбинированное) |
| **Интерфейс** | `[PRT]` | Тип связи | `MB` (Modbus), `PB` (Profibus), `ET` (Ethernet), `NO` (без связи) |

---

## 📝 Примеры кодировок

### Пример 1: Стандартный коммутационный аппарат
```
GX-M400-EM-NS-IP65-FM-MB
```
- **400А** номинальный ток
- **EM** электронный расцепитель  
- **NS** нержавеющая сталь
- **IP65** защита от пыли и воды
- **FM** комбинированное (местное + дистанционное) управление
- **MB** Modbus RTU/TCP интерфейс

### Пример 2: Аппарат для сухих помещений
```
GX-M250-TM-CS-IP54-RD-NO
```
- **250А** номинальный ток
- **TM** термомагнитный расцепитель
- **CS** стальной корпус
- **IP54** защита от пыли и брызг
- **RD** дистанционное управление
- **NO** без связи

### Пример 3: Взрывозащищённый аппарат
```
GX-M630-EM-EX-IP66-FM-ET
```
- **630А** номинальный ток
- **EM** электронный расцепитель
- **EX** взрывозащищённый корпус
- **IP66** полная герметичность
- **FM** полное управление
- **ET** Ethernet интерфейс

---

## 💻 Реализованные изменения

### 1. `src/services.CodeGenerator.ts`

#### Новые типы данных
```typescript
export interface GXCodeParams {
  current: number
  tripUnit: 'TM' | 'EM'
  body: 'CS' | 'NS' | 'EX'
  ip: string
  control: 'ML' | 'RD' | 'FM'
  protocol: 'MB' | 'PB' | 'ET' | 'NO'
}
```

#### Основная функция генерации
```typescript
export function generateGXCode(params: GXCodeParams): string {
  return `GX-M${params.current}-${params.tripUnit}-${params.body}-${params.ip}-${params.control}-${params.protocol}`
}
```

#### Функция маппирования заказа
Функция `mapOrderToGXCode()` автоматически:
- Преобразует тип расцепителя (TM/EN/EM → TM/EM)
- Определяет материал корпуса из данных корпуса
- Получает степень защиты IP из параметров корпуса
- Извлекает тип управления из параметров управления
- Определяет интерфейс из выбранных интерфейсов

### 2. `src/components/ConfiguratorForm.tsx`

#### Динамическое отслеживание всех полей
```typescript
const watchBase = watch('base')
const watchRelease = watch('release')
const watchEnclosure = watch('enclosure')
const watchControls = watch('controls')
const watchInterfaces = watch('interfaces')
```

#### Вычисляемый код с автоматическим обновлением
```typescript
const gxCode = useMemo(() => {
  return mapOrderToGXCode({
    device: {
      base: watchBase,
      release: watchRelease,
      enclosure: watchEnclosure,
      controls: watchControls,
      interfaces: watchInterfaces
    }
  })
}, [watchBase, watchRelease, watchEnclosure, watchControls, watchInterfaces])
```

#### Расшифровка компонентов кода
```typescript
const codeExplanation = useMemo(() => {
  const parts = gxCode.split('-')
  const explanations = {
    'TM': 'Термомагнитный расцепитель',
    'EM': 'Электронный расцепитель',
    'CS': 'Стальной лист',
    'NS': 'Нержавеющая сталь',
    'EX': 'Взрывозащищённый Ex-корпус',
    'ML': 'Местное управление',
    'RD': 'Дистанционное управление',
    'FM': 'Комбинированное управление (full mode)',
    'MB': 'Modbus',
    'PB': 'Profibus',
    'ET': 'Ethernet',
    'NO': 'Без связи'
  }
  return {
    prefix: parts[0],
    current: parts[1],
    tripUnit: parts[2],
    body: parts[3],
    ip: parts[4],
    control: parts[5],
    protocol: parts[6],
    explanations
  }
}, [gxCode])
```

#### UI-компоненты для отображения
- **Основной код**: отображается в виде `GX-M400-EM-NS-IP65-FM-MB`
- **Расшифровка**: сетка из 7 строк с объяснением каждого компонента
- **Примеры**: 3 готовых примера кодировок с описанием

---

## 🎯 Функциональность

### Автоматическое обновление кода
Код обновляется **в реальном времени** при изменении:
- ✅ Номинального тока (`base.current`)
- ✅ Типа расцепителя (`release.type`)
- ✅ Материала корпуса (`enclosure.material`)
- ✅ Степени защиты (`enclosure.ip`)
- ✅ Типа управления (`controls.controlType`)
- ✅ Интерфейсов (`interfaces[]`)

### Маппирование значений
| Источник | Целевое значение |
|----------|-----------------|
| `TM` расцепитель | `TM` (термомагнитный) |
| `EN/EM` расцепитель | `EM` (электронный) |
| `stainless` материал | `NS` (нержавейка) |
| `explosive` материал | `EX` (взрывозащита) |
| `steel` материал | `CS` (углеродистая сталь) |
| `local` управление | `ML` (местное) |
| `remote` управление | `RD` (дистанционное) |
| `combined` управление | `FM` (комбинированное) |
| Интерфейсы с `Modbus` | `MB` |
| Интерфейсы с `Profibus` | `PB` |
| Интерфейсы с `Ethernet` | `ET` |
| Нет интерфейсов | `NO` |

---

## 🧪 Примеры использования в коде

### Использование в форме
```typescript
// Автоматически генерируется при изменении любого поля
const gxCode = useMemo(() => {
  return mapOrderToGXCode({ device: {...} })
}, [watchBase, watchRelease, watchEnclosure, watchControls, watchInterfaces])
```

### Сохранение в заказе
```typescript
const items: OrderItem[] = [
  { code: gxCode, name: `Автоматический выключатель ${gxCode}`, qty: 1 }
]
```

---

## 📌 Важные замечания

1. **Дефолтные значения**:
   - Ток: 400А
   - Корпус: `CS` (углеродистая сталь)
   - IP: `IP40` (если не указано в каталоге)
   - Управление: `ML` (местное)
   - Интерфейс: `NO` (без связи)

2. **Порядок обработки интерфейсов**:
   - Проверяется первый интерфейс из массива
   - Поддерживаются: Modbus, Profibus, Ethernet, EthernetIP

3. **Расширяемость**:
   - Легко добавить новые типы корпусов
   - Легко добавить новые интерфейсы
   - Тип данных `GXCodeParams` позволяет расширение

---

## ✅ Проверка реализации

- ✓ TypeScript типы определены правильно
- ✓ Нет ошибок компиляции
- ✓ Функция `generateGXCode()` работает со всеми параметрами
- ✓ Функция `mapOrderToGXCode()` правильно маппирует значения
- ✓ Форма отслеживает все необходимые поля
- ✓ UI корректно отображает код и расшифровку
- ✓ Код обновляется динамически при изменении полей

---

## 🚀 Использование

При заполнении формы конфигуратора:
1. Выберите номинальный ток
2. Выберите тип расцепителя
3. Выберите материал/исполнение корпуса
4. Выберите степень защиты
5. Выберите тип управления
6. Выберите интерфейсы

**Результат**: Код автоматически генерируется и отображается в формате `GX-M[XXX]-[R]-[C]-[IP]-[UC]-[PRT]`
