# 📤 Как загрузить проект на GitHub (для начинающих)

Пошаговая инструкция на русском.

---

## Шаг 1: Установить Git (если ещё не установлен)

### На Windows
1. Откройте https://git-scm.com/download/win
2. Скачайте и установите
3. Принимайте всё по умолчанию
4. Перезагрузитесь

---

## Шаг 2: Открыть PowerShell в папке проекта

1. Найдите папку `gx-m400-configurator`
2. Нажмите **правый клик** → **"Open PowerShell here"**

Должно открыться чёрное окно командной строки.

---

## Шаг 3: Инициализировать Git репозиторий

Скопируйте и выполните эту команду в PowerShell:

```powershell
git init
```

Увидите сообщение о том что репо создалось.

---

## Шаг 4: Добавить файлы

```powershell
git add .
```

Эта команда добавляет ВСЕ файлы в очередь для загрузки.

---

## Шаг 5: Первый commit (снимок кода)

```powershell
git commit -m "Initial commit: GX-M Configurator ready for deployment"
```

Вы создали первый "снимок" вашего кода. `-m` это сообщение что вы сделали.

---

## Шаг 6: Создать репозиторий на GitHub

1. Откройте https://github.com/new
2. Заполните:
   - **Repository name**: `gx-m-configurator`
   - **Description**: "Configuration tool for GX-M mining equipment" (опционально)
   - **Public**: выберите ☑️
3. **НЕ выбирайте** "Initialize this repository with a README"
4. Нажмите **"Create repository"**

---

## Шаг 7: Подключить GitHub к вашему локальному репо

GitHub покажет команды. **Найдите секцию "…or push an existing repository from the command line"** и скопируйте 3 команды:

```powershell
git remote add origin https://github.com/ВАШ_ЛОГИН/gx-m-configurator.git
git branch -M main
git push -u origin main
```

**Замените `ВАШ_ЛОГИН` на ваш реальный логин GitHub!**

Вставьте в PowerShell и нажмите Enter.

---

## Шаг 8: Авторизация

Если спросит пароль:
- **Логин**: ваш логин GitHub
- **Пароль**: **НЕ ваш пароль**, а**Personal Access Token**

### Как создать Personal Access Token?

1. Откройте https://github.com/settings/tokens
2. Нажмите "Generate new token"
3. Выберите "Generate new token (classic)"
4. Заполните:
   - **Token name**: `gx-m-configurator-deploy`
   - **Expiration**: 30 дней
   - **Select scopes**: выберите `repo`
5. Нажмите "Generate token"
6. **Скопируйте токен** (больше не покажет!)
7. Вставьте в PowerShell когда спросит пароль

---

## Шаг 9: Проверка

Когда загрузка закончится, откройте ваш репо на GitHub:

```
https://github.com/ВАШ_ЛОГИН/gx-m-configurator
```

Должны видеть все ваши файлы! ✅

---

## 🎉 Готово!

Теперь переходите к **DEPLOYMENT_STEP_BY_STEP.md** для развёртывания на Vercel!

---

## ⚠️ Если что-то не работает

### "Permission denied"
→ Вы неправильно скопировали команду или токен. Проверьте ещё раз.

### "fatal: not a git repository"
→ Вы не в папке `gx-m-configurator`. Нажмите ещё раз "Open PowerShell here" в правильной папке.

### "Cannot find module"
→ Попробуйте `npm install` перед тем как делать git.

---

## 💡 Советы

- Если что-то пошло не так → просто удалите папку `.git` (скрытая папка, включите "Show hidden files") и начните сначала
- Personal Access Token храните в секрете, не делитесь им
- После загрузки GitHub репо, не трогайте `.git` папку

---

**После того как убедитесь что код на GitHub — переходите к [DEPLOYMENT_STEP_BY_STEP.md](./DEPLOYMENT_STEP_BY_STEP.md)!** 🚀
